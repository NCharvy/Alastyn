{% extends "AlastynAdminBundle::layout.html.twig" %}

{% block content %}
	<main style="overflow : hidden;">
        <div class="col-sm-6 col-sm-offset-3">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h2 style="text-align : center;">Enregistrement de flux RSS</h2>
                </div>
                <div class="panel-body">
                    <div class="col-md-10 col-md-offset-1">
                        {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
                        {{ form_errors(form) }}
                        <div class="form-group">
                            {{ form_label(form.url, "Lien RSS", {'label_attr': {'class': 'col-sm-5 control-label'}}) }}
                            {{ form_errors(form.url) }}
                            <div class="col-sm-6">
                                {{ form_widget(form.url, {'attr': {'class': 'form-control'}}) }}
                            </div>
                        </div>
                        <div class="form-group">
                            {{ form_label(form.domaine, "Domaine", {'label_attr': {'class': 'col-sm-5 control-label'}}) }}
                            {{ form_errors(form.domaine) }}
                            <div class="col-sm-6">
                                {{ form_widget(form.domaine, {'attr': {'class': 'form-control'}}) }}
                            </div>
                        </div>
                        <div class="form-group">
                            {{ form_label(form.publication, "Autoriser la publication ?", {'label_attr': {'class': 'col-sm-5 control-label'}}) }}
                            {{ form_errors(form.publication) }}
                            <div class="col-sm-6">
                                {{ form_widget(form.publication) }}
                            </div>
                        </div>
                        <div class="col-md-3 col-md-offset-8">
                            {{ form_widget(form.enregistrer, {'attr': {'class': 'btn btn-primary'}}) }}
                        </div>
                        {{ form_rest(form) }} {# Génération automatique de champs pas encore écrits, comme ceux générés automatiquement par Symfony #}
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </main>
{% endblock %}